{
  "name": "@aso/server",
  "version": "0.1.0",
  "description": "ASOptimus server (brain + till): orchestrator, core, llm-proxy, billing, auth, db, api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/main.ts",
    "migrate": "bun run src/db/migrate.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "nodemailer": "^6.9.14",
    "postgres": "^3.4.4",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/nodemailer": "^6.4.15"
  }
}
