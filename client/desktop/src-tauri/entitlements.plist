<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for Developer ID signing + notarization (NOT App Store; not sandboxed).
  The sidecar is the Bun runtime (JavaScriptCore JIT), so JIT + unsigned executable memory are required,
  and library validation is disabled so the app can exec the separately-embedded sidecar binary.
  network.client is harmless for a non-sandboxed app (WSS/HTTPS to the cloud).
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
