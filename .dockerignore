# Docker context = repo root, but the server image only needs server/ and shared/.
# Exclude everything else so the context doesn't bloat.
.git
.gitignore
.DS_Store
_legacy/
**/node_modules/
**/dist/
**/target/
client/
infra/
spec/
*.md
